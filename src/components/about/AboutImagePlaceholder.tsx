import {
  ImagePlaceholder,
  type ImagePlaceholderProps,
} from "@/components/ui/ImagePlaceholder";

export function AboutImagePlaceholder(props: ImagePlaceholderProps) {
  return <ImagePlaceholder {...props} />;
}
